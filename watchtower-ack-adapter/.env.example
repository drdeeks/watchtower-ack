# Character Kit facing
# Path to the root/user-owned enforcement socket the daemon listens on
# (Unix socket path, or tcp://host:port on Windows/cross-host).
CHARACTER_KIT_SOCKET=/run/ack/enforcer.sock
# Must match the daemon's ACK_AUTH_TOKEN, if it has one set. Required by
# every daemon method except "status".
CHARACTER_KIT_TOKEN=

# Watchtower facing (machine ingress)
WATCHTOWER_GATEWAY=https://fapi.drdeeks.xyz
WATCHTOWER_INGESTION_SECRET=
WATCHTOWER_PRODUCER=bridge-character-kit

# Watchtower facing (canonical, MOD-004) -- both optional. Absent, the
# bridge is legacy-HMAC-only, unchanged from before these fields existed.
# fw_agent_* enables FederationAgentClient for connect/heartbeat/events/
# disconnect; fw_owner_* is reserved for a future owner-bootstrap flow, not
# read by any code path yet.
WATCHTOWER_AGENT_TOKEN=
WATCHTOWER_OWNER_TOKEN=

# Bridge identity
BRIDGE_PROJECT_ID=
BRIDGE_AGENT_ID=

# Delivery hardening
BRIDGE_MAX_RETRIES=5
BRIDGE_RETRY_BASE_MS=200
